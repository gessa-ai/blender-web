<!--
SPDX-FileCopyrightText: 2026 blender-web contributors
SPDX-License-Identifier: GPL-3.0-or-later

Generated by scripts/public-dashboard.py from committed receipt and ledger inputs.
Do not edit by hand. Regenerate: scripts/public-dashboard.py --source HEAD
-->

# Parity and limitations

This is the public, fail-closed proof page for the browser port. It compares the current committed receipts with the pinned upstream target; missing or malformed evidence is shown as `UNAVAILABLE`, never inferred from a demo or an older run.

Pinned upstream: `fbe6228777e7 blender-v5.2-release (Blender 5.2 LTS)`.

The project is an independent derivative work. Not affiliated with, endorsed by, or sponsored by the Blender Foundation. Blender® is a registered trademark of the Blender Foundation. [Preferred-form GPL source](https://github.com/gessa-ai/blender-web).

## Current milestone receipts

**1 PASS · 7 FAIL · 1 UNAVAILABLE.** A component result does not promote its milestone; only the strict receipt row does.

| Milestone | Scope | Status | Passing checks | Non-passing checks | Receipt time (UTC) |
|---|---|---|---:|---|---|
| M0 | Toolchain + native oracle | PASS | 6/6 (100.0%) | — | 2026-08-20T18:55:37Z |
| M1 | Core boot + tier-(a) oracle | FAIL | 0/1 (0.0%) | strict_m1_receipt | 2026-08-20T18:55:37Z |
| M2 | Dependencies + Python boot | FAIL | 0/1 (0.0%) | strict_m2_receipt | 2026-08-20T18:55:38Z |
| M3 | WebGPU backend | FAIL | 0/1 (0.0%) | strict_m3_receipt | 2026-08-20T18:55:38Z |
| M4 | First pixels in a browser tab | FAIL | 0/1 (0.0%) | browser_pixels | 2026-08-20T18:55:38Z |
| M5 | Interactive parity | FAIL | 0/1 (0.0%) | interaction_parity | 2026-08-20T18:55:38Z |
| M6 | Render parity | FAIL | 0/1 (0.0%) | render_parity | 2026-08-20T18:55:38Z |
| M7 | Files + staged pipeline | FAIL | 0/1 (0.0%) | files_pipeline | 2026-08-20T19:47:20Z |
| M8 | Technical release package | UNAVAILABLE | unavailable | receipt missing | unavailable |

## Audited component evidence

These measured component counts add detail, but they do not override a red or unavailable milestone receipt.

| Component | Passing | Non-passing / boundary |
|---|---:|---|
| Native WebGPU GPU census | 148/158 (93.7%) | 8 fail / 2 crash; dispositions remain in the registry |
| Static shader translation | 951/973 (97.7%) | 22 non-passing; see named deferrals and blacklist evidence |
| Cross-built dependency inventory | 37 recorded | License and rationale are tracked in `ledger/deps.json` |

## Complete deferral registry

**53 registry entries** — deferred 38, deferred-by-goal 5, detector-active 1, partial 2, prototype-proven 1, resolved 6. Resolved rows remain visible as audit history; every active limitation names its blocker and revisit condition.

| id | status | scope | named blocker | revisit condition |
|---|---|---|---|---|
| wasm-fp-exception-status | resolved | M1 | Resolved: WebAssembly still has no floating-point exception status register, but BLI_expr_pylike_eval no longer depends on it under Emscripten. The canonical source accumulates explicit divide-by-zero and invalid-operation status through constant folding and runtime evaluation. | closed by the canonical source's software exception accumulator; retain this row as resolved audit history |
| float32-ulp-mathutils | deferred | M2 (m2b tier-b) | Blender's vector/matrix math is float32; the wasm libm (acosf/sqrtf/etc.) rounds differently from native arm64, so last-ULP numeric divergences appear versus the native oracle. | m2b — relaxed wasm tolerance or a libm review |
| wasm32-64bit-blend-collision | detector-active | M2 (ADR-004) | Reading 64-bit .blend files on wasm32 truncates 8-byte block addresses into 4 bytes assuming 8-byte alignment (>>3 at BLO_core_bhead.hh:124 / dna_genfile.cc:929). Real writers emit sub-8-aligned adjacent DATA blocks whose distinct addresses then collide → duplicate map insert (loud abort) or mis-resolved pointer fields (silent corruption). Data-dependent: most files pass, some break. | structural fix is probe-gated (wasm64 -sMEMORY64 vs wasm32 pointer interning), likely post-M4 / pre-launch; LAUNCH.md drag-drop depends on it landing before M8 |
| snorm16-texture-formats | deferred | M3 (T9 pre) | WebGPU/Dawn exposes 16-bit UNORM formats (Unorm16TextureFormats feature) but has NO 16-bit SNORM support at this pin: RGBA16Snorm/R16Snorm/RG16Snorm enumerators exist in webgpu_cpp.h yet no FeatureName enables them, so CreateTexture fails validation. Metal supports MTLPixelFormatRGBA16Snorm — this is a Dawn/WebGPU-surface gap, not hardware. | M3 T9 texture integration — emulate, or confirm no shipping shader needs snorm16 |
| pointsize-shader-expansion | prototype-proven | M3 (pointsize pre) | WebGPU points are always 1px; WGSL has no gl_PointSize builtin and no gl_PointCoord. Every GPU_PRIM_POINTS shader that writes gl_PointSize must be rewritten to draw an instanced quad per point. | M5/M6 — wgpu_batch point-draw expansion + BSL preprocessor prologue (UI/overlay dots, keyframes, particles, edit-mode verts) |
| feature-off-ik-solvers | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_IK_SOLVER and WITH_IK_ITASC are both forced OFF, leaving the IK plugin table without either solver implementation. | Post-launch, cross-build and validate both the legacy IK solver and iTaSC in browser animation parity sessions. |
| feature-off-bullet-physics | deferred | M8 launch registry | Chosen browser launch scope cut: Bullet is not cross-built or linked and WITH_BULLET is forced OFF. | Post-launch, cross-build Bullet and add rigid-body plus Bullet-backed geometry parity coverage. |
| feature-off-ocean-modifier | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_MOD_OCEANSIM and its FFTW3 dependency are forced OFF. | Post-launch, cross-build the ocean/FFTW dependency path and add modifier parity scenes. |
| feature-off-remesh-quadriflow | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_MOD_REMESH and WITH_QUADRIFLOW are forced OFF. | Post-launch, enable the Remesh modifier and cross-build Quadriflow with focused topology parity tests. |
| feature-off-exact-boolean | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_MANIFOLD and WITH_GMP are forced OFF, so the exact boolean implementation is not linked. | Post-launch, cross-build and validate the Manifold or GMP exact-boolean implementation. |
| feature-off-slim-uv | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_UV_SLIM is forced OFF. | Post-launch, enable SLIM and add minimum-stretch UV parity coverage. |
| feature-off-video-ffmpeg | deferred | M8 launch registry | Chosen browser launch scope cut: FFmpeg is not harvested for wasm and WITH_CODEC_FFMPEG is forced OFF. | Post-launch, cross-build FFmpeg under a separately reviewed payload, codec, and browser-media policy. |
| feature-off-audio | deferred | M8 launch registry | Chosen browser launch scope cut: Audaspace, libsndfile, every native audio device backend, and Rubber Band are forced OFF; no Web Audio bridge exists. | Post-launch, design a browser audio bridge and cross-build the required Audaspace/codec subset. |
| feature-off-fbx-io | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_IO_FBX is forced OFF. | Post-launch, cross-build and validate Blender's native FBX IO module. |
| feature-off-alembic-io | deferred | M8 launch registry | Chosen browser launch scope cut: Alembic is not cross-built and WITH_ALEMBIC is forced OFF. | Post-launch, cross-build Alembic and add cache import/export parity coverage. |
| feature-off-grease-pencil-vector-io | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_IO_GREASE_PENCIL, WITH_HARU, and WITH_POTRACE are forced OFF. | Post-launch, cross-build the vector-IO dependencies and validate Grease Pencil SVG/PDF operators. |
| feature-off-openimagedenoise | deferred | M8 launch registry | Chosen browser launch scope cut: OpenImageDenoise is not cross-built and WITH_OPENIMAGEDENOISE is forced OFF. | Post-launch, cross-build OpenImageDenoise and validate CPU/browser denoise memory and latency budgets. |
| feature-off-freestyle | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_FREESTYLE is forced OFF and Freestyle render modules are not linked. | Post-launch, enable Freestyle and add line-render parity scenes. |
| feature-off-motion-tracking | deferred | M8 launch registry | Chosen browser launch scope cut: LibMV is not cross-built and WITH_LIBMV is forced OFF. | Post-launch, cross-build LibMV and restore movie-clip tracking/solve registration with browser media coverage. |
| feature-off-openxr | deferred | M8 launch registry | Chosen browser launch scope cut: the native OpenXR path has no browser/WebXR bridge and WITH_XR_OPENXR is forced OFF. | Post-launch, design an explicit WebXR-to-GHOST path rather than enabling the native OpenXR backend unchanged. |
| feature-off-jpeg2000-webp-dpx | deferred | M8 launch registry | Chosen browser launch scope cut: WITH_IMAGE_OPENJPEG, WITH_IMAGE_WEBP, and WITH_IMAGE_CINEON are forced OFF. | Post-launch, cross-build and test the OpenJPEG/WebP/Cineon-DPX codec paths against browser memory and payload budgets. |
| ime-dead-keys | partial | M4 (GHOST-web design) | Terminal recovery is implemented, but raw key listeners are canvas-only and the hidden textarea clears non-composing input without forwarding it. Current composition tests also dispatch untrusted synthetic CompositionEvent objects; no trusted browser-generated OS IME/dead-key receipt exists. | M4 input closure: first route ordinary non-composing keyboard/control input while Blender's IME textarea owns focus, then obtain a trusted physical IME/dead-key receipt on a supported browser/OS |
| ghost-web-multi-window | deferred | M4 (GHOST-web design) | The browser backend intentionally owns one OffscreenCanvas and one window_ callback/input/hit-test/presentation owner, with no simultaneous per-window routing. createWindow now rejects a second live request before constructing its context; replacement remains available after the first window is disposed. | post-launch, or when one canvas, input, presentation, and callback owner can be provided for every simultaneous GHOST window |
| node-ungroup-socket-flake | resolved | M2 (m2b) | Non-deterministic (~1.4%, ~1 in 70): after the node-group ungroup operator on the threaded wasm profile (-sPROXY_TO_PTHREAD + TBB), a ShaderNodeValue proxy node's output 'Value' socket intermittently resolves to NodeSocketUndefined instead of NodeSocketFloat. Deterministic + correct on the native oracle and on the direct-add control (both wasm and native). Leading hypotheses: a data race (GN eval on TBB workers concurrent with the next operator's tree mutation) or an uninitialized read of a freed-then-reused bNodeSocket field. | pending the hunt's outcome — one instrumented build + a ~150-run background hunt to separate race vs uninitialized-read (fix is copy-on-eval/tree-lock isolation if race, zero-init the reused socket field if uninit) |
| shared-node-clipboard-path | deferred | M2 (m2b, secondary bug) | Node clipboard copy writes to a SHARED copybuffer temp path (node_copybuffer_filepath_get, clipboard.cc:48). Concurrent/multi-context processes collide on that path so write_as_copypaste_buffer returns false and node_clipboard_copy_exec hits BLI_assert (clipboard.cc:241, 'marked unreachable'). | M7 / multi-tab — per-context copybuffer path |
| optional-python-modules | deferred | M2 (python boot) | The browser CPython target deliberately omits optional extensions that require unharvested native dependencies or unsupported process/terminal facilities: _ctypes (libffi), _ssl (OpenSSL), _lzma (xz), _sqlite3, _bz2, the C _decimal accelerator, pyexpat/_elementtree, readline/_curses, and _uuid. A thread-backed _multiprocessing compatibility shim supports synchronization imports but does not provide native process spawning. | Post-launch, add each external dependency only when a product-supported script requires it; keep the mono-wasm/no-dlopen constraint explicit. |
| cycles-final-gpu | deferred-by-goal | GOAL | No WebGPU hardware ray tracing / bindless resources, so Cycles-GPU final rendering cannot be ported. Cycles-GPU device paths forced OFF (ledger/deps.json forced_off cycles_gpu_devices/embree). | post-launch — needs WebGPU hardware ray tracing / bindless (not available); launch ships Cycles-CPU small scenes only |
| osl | deferred-by-goal | GOAL | Open Shading Language needs LLVM JIT, which the browser sandbox does not permit. LLVM/OSL forced OFF (ledger/deps.json forced_off llvm/cycles_osl). | post-launch — needs a JIT (unavailable in the browser sandbox) |
| mantaflow | deferred-by-goal | GOAL | Mantaflow (fluid/smoke) is not ported; MOD_FLUID forced OFF (ledger/deps.json forced_off mod_fluid). | no port planned |
| large-scenes-16gb | deferred-by-goal | GOAL | Scenes above ~16 GB exceed the Memory64 addressing cap for the wasm build. | post-launch — coupled to the wasm64/Memory64 decision (see ADR-004 Decision 3) |
| os-shell-affordances | deferred-by-goal | GOAL | OS-shell affordances (spawning external processes, arbitrary filesystem access outside the sandbox, native file dialogs beyond FSA) are unavailable in the browser sandbox. | no port — browser sandbox constraint |
| wasm32-animation-action-objectdata | deferred | M2 | The wasm32 animation action suite completes all 32 assertions but its library-override reload reports one missing local ObjectData and cannot find the Suzanne library object data; native does not emit either diagnostic. | with the ADR-004 wasm64 path or a focused library-override remap fix |
| wasm32-library-override-idname-allocation | deferred | M2 | The wasm32 library-override suite passes every assertion but allocates six LibCube local-ID suffix correspondences across three equivalent collection instances differently from native. | with the ADR-004 wasm64 path or deterministic local-ID allocation work |
| feature-off-openvdb | deferred | unavailable | OpenVDB not built for wasm (WITH_OPENVDB OFF) | post-launch |
| feature-off-opensubdiv | resolved | M6 | Resolved: OpenSubdiv 3.7.0 is cross-built and the shipping web configuration forces WITH_OPENSUBDIV ON for CPU Subsurf/Multires and Cycles subdivision. | closed by the M6 OpenSubdiv dependency rebuild |
| feature-off-cycles-engine | resolved | M6 | Resolved for the launch-tier CPU subset: the shipping web configuration forces WITH_CYCLES ON while every unsupported GPU device and OSL remain explicitly off. | closed for Cycles CPU; do not collapse the separate GPU/OSL deferrals |
| feature-off-avif | deferred | unavailable | libaom not harvested | M7 |
| storage-texture-atomics | deferred | M3 (gate) | WGSL has no storage-texture atomics: imageAtomic* maps to SPIR-V OpImageTexelPointer, which Tint's SPIR-V reader rejects ('tint ReadIR: unhandled SPIR-V OpImageTexelPointer'). No Dawn FeatureName enables image atomics at this pin. | post-launch - WGSL has no storage-texture atomic path; revisit if WGSL/Dawn gains image atomics or an SSBO-atomic emulation of the affected passes is scoped |
| vertex-stage-rw-storage | deferred | M3 (gate) | WebGPU forbids var<storage, read_write> in the vertex pipeline stage; Tint rejects it: 'var storage/read_write cannot be used by vertex pipeline stage'. | post-launch - restructure the affected passes to move read_write storage writes into a compute prepass (vertex stage reads read-only storage only) |
| depth-aspect-buffer-upload | deferred | M3 (gate) | WebGPU forbids writing the depth aspect of a texture via a buffer->texture copy (buffer->depth-aspect copies are invalid). The clear + read-back paths work. | when a buffer->depth upload workflow is in scope - emulate via a fullscreen depth-write render pass instead of a buffer copy |
| gpu-comp-i10-vertex-format | resolved | M3 (gate) | WebGPU has no signed snorm10-10-10-2 vertex format; wgpu_pipeline.cc:86 maps GPU_COMP_I10 (signed 2_10_10_10_REV, used for packed mesh normals) to VF::Unorm10_10_10_2, so the unsigned read drops the sign. | closed by patch 0119: normalized I10 data is CPU-transcoded to Snorm8x4, a WebGPU-supported signed format |
| subpass-input-attachment | deferred | M3 (gate) | WebGPU has no subpass inputs / input-attachment equivalent. | M6 entry - REVISIT on EEVEE render evidence per fix_plan.md M3.F13-DECISIONS decision 2; 2-pass emulation designed in notes/gpu-laneB-integration.md |
| emscripten-network-transport | deferred | M4 | urllib3 2.4.0 hard-codes a Pyodide runtime on sys.platform=='emscripten' (urllib3/__init__.py:208-211 unconditionally runs inject_into_urllib3(); contrib/emscripten/fetch.py:45-46 imports js + pyodide.ffi, modules only the Pyodide RUNTIME provides). Our non-Pyodide browser CPython has no JavaScript bridge, so no Python-level HTTP transport exists. | extensions-online / networked features work (M7-adjacent): implement a real browser fetch transport backing the js + pyodide.ffi namespace (option B, notes/python-emcc605-probe.md THE NEXT WALL), or a native urllib3 emscripten transport |
| gpu-sync-readback-windowed | deferred | M5 | Window.screenshot() promises an immediate memoryview, but browser WebGPU mapping completes only after control returns to the WM-worker event loop. A same-thread wait prevents that completion, and ADR-006 deliberately forbids JSPI/Asyncify. Live C1/M5 proof separately has the named blocker no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn). | Revisit the Python Window.screenshot() immediate-memoryview contract only if ADR-006 deliberately enables a browser stack-suspension mechanism, or if Blender adds an asynchronous memoryview API with an upstream-compatible lifetime contract. Live C1 and aggregate M5 acceptance separately require a conformant hardware browser receipt under wsl2-hardware-webgpu-m5. |
| webgpu-sync-occlusion-query | deferred | M3 (gate) | WebGPU occlusion results require resolving a query set and asynchronously mapping a result buffer. Blender's legacy gpu_select_sample_query path calls QueryPool::get_occlusion_result synchronously inside GPU_select_end(), while ADR-006 forbids blocking the browser WM worker on the callback needed to complete that map. | M5 gizmo interaction parity: convert the legacy sample-query selection caller to an encode/resolve/poll continuation, or route browser gizmo picking through the asynchronous Select-Next path |
| usd-io-wasm | resolved | M7 | Resolved: OpenUSD 26.03 is cross-built for Emscripten and the shipping web configuration enables real Blender USD IO. The constrained profile omits Imaging/Hydra and Python hooks explicitly while retaining UsdGeomMesh import/export. | closed by the M7 OpenUSD 26.03 core-profile build and real browser operator round-trip |
| wsl2-hardware-webgpu-m3 | deferred | M3 | no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn) | Run the strict producer on the driver-operated Apple M4 Pro; this WSL lane must not retry dzn, Windows interop, or any reboot path. |
| wsl2-hardware-webgpu-m4 | deferred | M4 | no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn) | Run the current CAPTURE generation on the driver-operated Apple M4 Pro for P0-D/P0-E semantic pixels and accepted success/terminal profiles, then relink the hash-bound APPLY product. |
| wsl2-hardware-webgpu-m5 | deferred | M5 | no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn) | After accepted Apple CAPTURE profiles authorize APPLY, run interaction, click-pick, canvas, and latency producers on the driver-operated Apple M4 Pro. |
| wsl2-hardware-webgpu-m6-gpu | deferred | M6 | no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn) | After accepted Apple CAPTURE profiles authorize APPLY, run the Workbench and EEVEE matrices on the driver-operated Apple M4 Pro. |
| wsl2-hardware-webgpu-m7 | deferred | M7 | no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn) | After accepted Apple CAPTURE profiles authorize APPLY, run the strict files/pipeline browser producer on the driver-operated Apple M4 Pro. |
| wsl2-hardware-webgpu-m8 | deferred | M8 | no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships none; Mesa dzn rejected by Dawn) | After accepted Apple CAPTURE profiles authorize APPLY, run the hardware product/performance/soak matrix on the driver-operated Apple M4 Pro; keep the independent payload/latency bar red until measured green. |
| m8-wasm-15mb-bar | partial | M8 | The last exact staged browser receipt remains over both launch budgets: stage-0 does not reach product pixels; eventual interactive UI appears at 20.847 s locally and 41.519 s under the pinned mid-laptop 1.5 MB/s + 40 ms profile, versus <=8 s, and its complete critical payload is 37,840,484 bytes versus <=15,000,000. The current CAPTURE generation has no accepted hash-bound APPLY artifact or browser timing receipt. A fully replayed cross-generation hybrid planning fixture measures 14,628,429 bytes of complete critical wire, 371,571 bytes under the size ceiling: its earlier c9 provisional primary contributes 12,292,157 bytes and its exact current non-Wasm/control subtotal is 2,336,272 bytes. This includes the generated index/service-worker controls, removes the duplicate pthread worker artifact, and sets a 12,663,728-byte maximum current primary, but it is not current-generation APPLY evidence. The accepted Apple profiles have 136,751 counters while the current b8b2a682ff09 CAPTURE original has 136,754 defined functions and shifted protected-function ordinals; rewriting their checksum or padding counters would misclassify functions. Previously accepted Apple profiles are therefore structurally incompatible with the current relink. Functional staging, byte-exact Stage 1, visible phase/MB progress, cache completion, and offline-warm behavior do not override the missing exact APPLY size and <=8 s receipts. | Immediately after fresh current-generation Apple profiles authorize APPLY: assemble the exact public tree, run three cold 1.5 MB/s + 40 ms hardware passes, and require complete critical wire <=15,000,000 plus decoded semantic interaction <=8 seconds before changing this row. |

## Reproducibility

- Generator: `scripts/public-dashboard.py --source HEAD`
- Receipt sources: `ledger/results/*.json`
- Limitation source: `ledger/deferred.json`
- Public input SHA-256: `0d0957bb7847c16cc3ecd39e81f8352eee4d28b70e0509c0515ed53ac7044922`
