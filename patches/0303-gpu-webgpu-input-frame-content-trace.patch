diff --git a/source/blender/gpu/webgpu/wgpu_context.cc b/source/blender/gpu/webgpu/wgpu_context.cc
index d2ea7b1fbeea30d6775e8d7f528ff90f6c373d82..a353b8c741bbd9d81200f9d606daf8d754fc23ce 100644
--- a/source/blender/gpu/webgpu/wgpu_context.cc
+++ b/source/blender/gpu/webgpu/wgpu_context.cc
@@ -1393,6 +1393,10 @@ void WGPUContext::begin_frame()
   /* Activation already adopted this frame's backbuffer episode. Reset its semantic trace once at
    * the real WM frame boundary; later context reactivations must not erase earlier region draws. */
   ghost_web::redraw_trace_frame_begin(redraw_present_frame_episode_);
+  /* Bind strict post-input content evidence to this same WM frame. A terminal callback is eligible
+   * only after its synthetic WindowUpdate has crossed Blender's GHOST consumer. */
+  ghost_web::input_redraw_trace_frame_begin(ghost_web::input_redraw_dispatched_count(),
+                                            ghost_web::input_redraw_terminal_count());
 #endif
   queue_scheduler_.begin_epoch();
 }
diff --git a/source/blender/gpu/webgpu/wgpu_framebuffer.cc b/source/blender/gpu/webgpu/wgpu_framebuffer.cc
index 7d10bd581afd9e02afa91af28867c277540c8658..b31171bb35c53e699214e4f83064555d1cba10ea 100644
--- a/source/blender/gpu/webgpu/wgpu_framebuffer.cc
+++ b/source/blender/gpu/webgpu/wgpu_framebuffer.cc
@@ -1224,7 +1224,10 @@ bool WGPUFrameBuffer::load_pass_viewport_plan(
 void WGPUFrameBuffer::debug_note_draw(const char *shader_name) const
 {
 #ifdef __EMSCRIPTEN__
-  if (!ghost_web::redraw_trace_capturing() || multi_viewport_) {
+  if ((!ghost_web::redraw_trace_capturing() &&
+       !ghost_web::input_redraw_trace_capturing()) ||
+      multi_viewport_)
+  {
     return;
   }
   webgpu::FramebufferViewportPlan draw_viewport;
@@ -1242,19 +1245,42 @@ void WGPUFrameBuffer::debug_note_draw(const char *shader_name) const
     pass = ghost_web::RedrawTracePass::OcioDisplay;
   }
   WGPUContext *wctx = active_context();
+  const bool window_target = wctx != nullptr && wctx->is_window_backbuffer(this);
+  const int32_t viewport_x = draw_viewport.viewport.viewport_x;
+  const int32_t viewport_y = draw_viewport.viewport.viewport_y;
+  const uint32_t viewport_width = draw_viewport.viewport.viewport_width;
+  const uint32_t viewport_height = draw_viewport.viewport.viewport_height;
+  const bool scissor_enabled = draw_viewport.scissor_enabled;
+  const uint32_t scissor_x = draw_viewport.scissor_x;
+  const uint32_t scissor_y = draw_viewport.scissor_y;
+  const uint32_t scissor_width = draw_viewport.scissor_width;
+  const uint32_t scissor_height = draw_viewport.scissor_height;
   ghost_web::redraw_trace_note(pass,
-                               wctx != nullptr && wctx->is_window_backbuffer(this),
+                               window_target,
                                width_,
                                height_,
-                               draw_viewport.viewport.viewport_x,
-                               draw_viewport.viewport.viewport_y,
-                               draw_viewport.viewport.viewport_width,
-                               draw_viewport.viewport.viewport_height,
-                               draw_viewport.scissor_enabled,
-                               draw_viewport.scissor_x,
-                               draw_viewport.scissor_y,
-                               draw_viewport.scissor_width,
-                               draw_viewport.scissor_height);
+                               viewport_x,
+                               viewport_y,
+                               viewport_width,
+                               viewport_height,
+                               scissor_enabled,
+                               scissor_x,
+                               scissor_y,
+                               scissor_width,
+                               scissor_height);
+  ghost_web::input_redraw_trace_note(pass,
+                                     window_target,
+                                     width_,
+                                     height_,
+                                     viewport_x,
+                                     viewport_y,
+                                     viewport_width,
+                                     viewport_height,
+                                     scissor_enabled,
+                                     scissor_x,
+                                     scissor_y,
+                                     scissor_width,
+                                     scissor_height);
 #else
   (void)shader_name;
 #endif
diff --git a/source/blender/gpu/webgpu/wgpu_framebuffer.hh b/source/blender/gpu/webgpu/wgpu_framebuffer.hh
index 6abe2abef618e9aeeee9e91917c235abc0e3a3f0..21c2e8efdb0c6ac77224c0df39353c524741d031 100644
--- a/source/blender/gpu/webgpu/wgpu_framebuffer.hh
+++ b/source/blender/gpu/webgpu/wgpu_framebuffer.hh
@@ -104,9 +104,9 @@ class WGPUFrameBuffer : public FrameBuffer {
                                               webgpu::FramebufferLoadActionTransaction> &load_action,
                                           int force_layer = -1);

-  /** During a bounded browser-resize recovery episode, record the exact target and resolved
-   * viewport/scissor used by a successfully recorded draw. Native and steady-state paths are
-   * no-ops. */
+  /** During a bounded browser-resize or pending terminal-input trace, record the exact target and
+   * resolved viewport/scissor used by a successfully recorded draw. Native and steady-state paths
+   * are no-ops. */
   void debug_note_draw(const char *shader_name) const;

   /* --- Multi-viewport / layered emulation (M3.F7), read by wgpu_batch --- */
