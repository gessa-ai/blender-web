#!/usr/bin/env bash
# Harness v0 (M0 builds this out; locked via .claude/harness.lock at M0.5).
echo "harness v0: no suites yet — M0.5 implements --scope m0. Refusing to report green for anything."
exit 1
