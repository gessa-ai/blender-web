<!-- SPDX-FileCopyrightText: 2026 blender-web contributors -->
<!-- SPDX-License-Identifier: CC0-1.0 -->

# HANDOFF — 2026-08-08, mid-M4 (last mile) + M3 promised + M6/M7/M8 all in active parallel probes

Written because the driving session hit context limits after ~3 days of continuous
orchestration. This is a complete, self-contained state snapshot so a fresh session
(any model) can resume orchestrating the fleet with **zero context loss**. Read this
file first, then follow the onboarding order below exactly as CLAUDE.md/GOAL.md
prescribe — nothing about the standing authority has changed.

**Rate-limit note:** two subagents died this session with `weekly limit resets Aug 12
at 10am (America/New_York)` — that is a *subagent-pool* limit, not necessarily this
session's. If you (the new driver) hit the same wall immediately, stop and tell the
human plainly; do not retry in a loop. If you have budget, proceed — there is a huge
amount of well-characterized, ready-to-dispatch work below.

## Onboarding order (unchanged — read fresh every session)

1. `GOAL.md` — the contract. Sole authority. Re-read every iteration.
2. `notes/decisions.md` — D-1..D-8, do not re-litigate without new evidence.
3. `notes/orchestration.md` — dispatch patterns, verify-don't-trust, failure modes.
4. `fix_plan.md` — the live task list. **NOTE: its heading says "M1 CORE BOOTS" —
   this is STALE, ignore it; the dashboard (`scripts/dashboard.sh`) derives status
   correctly from result JSONs + literal `<promise>` tags, not this heading.** The
   M3/M4 sections (search `## M3` and `## M4`) are where the live task items are;
   most of the file below M2 is historical/DONE — skim, don't re-read closely.
5. `notes/adr/ADR-001..007*.md` — seven architecture decisions, all evidence-backed,
   all closed. ADR-006 (drop JSPI, windowed profile) and ADR-007 (WM-worker device
   lifecycle, tick-pumped futures) are THIS session's and directly govern the M4
   work below — read both in full, they are short.
6. `reports/checkpoint-20260806.md` — the last formal cadence report (still broadly
   accurate on strategy; numbers below in this file are newer).
7. `reports/audit-20260805.md` + `reports/audit-20260806.md` — two clean adversarial
   passes (0 critical, 0 major each). Due for a THIRD pass at ~25 more commits
   (audit cadence is every-25-iterations per GOAL.md); we're overdue-ish, do one
   when the queue allows.
8. This file, for the tactical state.

## Milestone status (verify via `harness/status.sh` — do this FIRST, trust it over prose)

- **M0 TOOLCHAIN + ORACLE — DONE.**
- **M1 CORE BOOTS + FREE ORACLE — DONE.** `<promise>M1_CORE_BOOTS</promise>` issued.
- **M2 DEPS + PYTHON BOOTS — DONE.** `<promise>M2_DEPS_PYTHON</promise>` issued.
- **M3 WEBGPU BACKEND — DONE, PROMISED.** `<promise>M3_GPU_BACKEND</promise>` issued
  2026-08-06 (commit `52ec8a5`, harness v1.5, `scope_m3` installed under the
  documented lock-lift procedure). Receipts: `harness/status.sh` → **m3 5/5 GREEN**.
  The scope permanently encodes: native `GPUWebGPUTest` suite **148 PASS / 8 FAIL /
  2 CRASH (158 tests)**, `static_shaders` **956/973** compiling, every non-pass
  mapped to a registered deferral (`ledger/deferred.json`, 23 entries total, 5 are
  M3-gate) or a documented blacklist (`notes/gpu-gate-blacklist.md`). **This gate
  is locked and green — do not touch `harness/run.sh` again without the lock-lift
  procedure in `notes/harness-issues.md`.**
- **M4 FIRST PIXELS IN A TAB — NOT YET PROMISED. This is the live front.** Full
  detail in the next section — it is very close. Blender's UI (splash, menus,
  toolbars, panels) has been rendering upright and interactive in a real Chrome tab
  since round 16-17; the last mile is the 3D viewport interior compositing content,
  which is now blocked on ONE THING (see below) that is **already fixed but sitting
  on an unmerged branch**.
- **M5 INTERACTIVE PARITY — prep DONE, not started.** 8/8 tier-(c) event-simulate
  sessions authored + cross-run deterministic against the native oracle
  (`sandbox/m5-prep/`, commit `eda1608`). Blocked on M4 (needs a stable windowed
  loop to replay sessions into).
- **M6 RENDER PARITY — prep DONE + first real data landed, active probes.**
  - Oracle goldens: 77 staged (workbench 20, EEVEE 30, Cycles 27), 72/77 oracle-green,
    idiff comparator self-tested with teeth (`sandbox/m6-prep/`, commit `ff36068`
    for the M4-specific golden variant, `1fe4360`/`98d3c37`-era for the main set).
  - **Cycles-CPU wasm: FIRST RENDER PASSED THE ORACLE COMPARATOR AT EXIT 0**
    (commit `365897a`+`b8e1840`) — the default cube, near-bit-exact (78.2 dB PSNR,
    0 pixels over Blender's own 0.016/1% threshold), **zero upstream patches
    needed**. This is a real milestone artifact; a PNG was sent to the human.
  - **Cycles-CPU suite MEASURED: 2/27 PASS as-built** (commit `6b3bcc3`), but **22
    of the 25 failures are PROVEN (via a controlled A/B re-render on the native
    oracle with Subsurf force-disabled) to collapse to ONE missing dependency:
    `WITH_OPENSUBDIV=OFF`**. The Cycles-CPU rendering path itself (shading, color
    management, sampling, ray tracing) is faithful. 3 residual failures
    (`principled_bsdf_default`, `emission_alpha`, `thin_subsurface`) are suspected
    Embree-vs-BVH2 (`WITH_CYCLES_EMBREE=OFF`) — re-characterize once OpenSubdiv is
    ON. **A worker was dispatched to build OpenSubdiv + flip the config + relink +
    re-measure and died at 0% progress on a rate limit (see "Dead subagents" below)
    — this is the single highest-value M6 task to redispatch.**
- **M7 FILES + PIPELINE — prep DONE on 3 fronts, no code changed yet.**
  - OPFS substrate proven (`notes/m7-opfs-probe.md`, commit `b9611cf`): 0.5-1 GB/s,
    persistence across reload, worker-only sync-handle coupling confirmed both
    directions.
  - **Project-store design + BOTH-HALVES-PROVEN** (`notes/m7-store-design.md`,
    commit `370c69f`): real `.blend` bytes byte-identical through OPFS across page
    reloads (fnv hash match), Blender's own path-resolution code honors an env
    recipe with **ZERO upstream edits** (`BLENDER_USER_RESOURCES=/projects` +
    `TMPDIR=/projects/.recovery` + one WasmFS OPFS-mount init call). **Integration
    steps are patch-ready** — this is now a "just wire it into the windowed shell"
    task, not a design task.
  - **IO export smoke: 2 blockers found, both cheap, both characterized**
    (`notes/m7-io-smoke.md`, commit `080df00`):
    1. Native C++ exporters (OBJ/USD/PLY/STL/FBX) are **compiled out** —
       `patches/blender_web.cmake:176-180` force-disables them. Launch tier needs
       OBJ+USD. Fix = flip `WITH_IO_WAVEFRONT_OBJ/PLY/STL=ON` + relink (same relink
       as the OpenSubdiv task above — **bundle them**, see redispatch below).
    2. glTF export (Python addon, the format your platform integration will likely
       use for game-engine hand-off) reaches real code then **aborts in numpy**:
       `Assertion failed: PyGILState_Check()` on first array alloc. Root cause:
       `lib/wasm` numpy 2.3.4 was built **without** `-DNDEBUG` (assertions on), and
       the assertion false-fires under the `-sPROXY_TO_PTHREAD` threading profile.
       **Fix = rebuild numpy in Release (`-DNDEBUG`)** — small, isolated,
       `scripts/deps/numpy.sh` (extend or add a release-mode invocation).
       Turnkey comparators exist (`sandbox/m7-io-smoke/parse_glb.py`,
       `parse_obj.py`) — once fixed, the verdict is one command.
  - Platform-integration design note (`notes/platform-integration-design.md`,
    commit `1ec663a`, corrected `2694fdf`): account-based model library, "Open
    with", auto-save — all map cleanly onto the OPFS store seams above. This is
    for the human's separate platform/game-engine product, **zero upstream
    patches needed**, arm's-length via URL/postMessage per the GPL-boundary
    caution recorded there.
- **M8 LAUNCH GATE — active measurement, one probe died at 0%.**
  - **Size baseline measured** (`notes/m8-size-probe.md`, commit `0c23a90`): first
    RELEASE-profile (`-O2 -g0`) windowed link. wasm 921→120 MB raw (7.7×), **whole
    app = 47.5 MB brotli wire**. Correctness clean at `-O2` (boots stable, patch
    0054's NaN helper survives, byte-identical codegen to the dev `-O2 -g` link
    minus DWARF — zero risk).
  - **Staged-loading probe DONE** (`notes/m8-staged-loading.md`, commit `5c53069`):
    **stage-0 (wire-to-first-interactive) = 23.6 MiB brotli** (wasm 20.1 + js 0.09
    + data 4.5), a 50% cut from the 47.5 MB monolith. Lazy-fetch of stage-1 data
    proven working (module fetched post-boot, imported live, no interpreter
    restart) on a real CDP-throttled 4G connection. **Found a free 40.8 MiB raw
    win: `.data` ships Python `.pyc` in 3 redundant optimization levels alongside
    the `.py` source — drop all `__pycache__`, keep `.py` only, zero correctness
    risk** (not yet implemented — just proven safe and sized).
  - **VERDICT: staged data alone is NOT enough.** The wasm module itself is 20.1 MB
    brotli — **already 34% over LAUNCH.md's 15 MB bar before any data loads.**
    Closing this needs compiler-level shrink (`-Os`/`-Oz`, `--closure`, LTO) or
    feature/DCE work. **A worker was dispatched to measure exactly this
    (`-Os`/`-Oz`/closure/LTO against the opt tree) and died at literally the
    FIRST action (examining cache entries) — zero data collected.** This is the
    other highest-value redispatch below.

## M4 — exactly where it stands (read this section twice, it's the live front)

**The headline: Blender's real UI has been rendering, upright, interactive, in a
real Chrome tab since round 16-17 (evidence: `platform_web/shell/evidence/m4-*`,
many PNGs, several sent to the human already). The remaining work is the 3D
viewport INTERIOR (cube+grid) compositing content, which the M4 gate (splash +
default cube vs the native golden within idiff threshold) requires.**

Timeline of the last-mile chase (rounds 16 through 24, all on `agent/m2.5-python-boot`):
- **r16-r19**: bind-group/pipeline machinery landed; UI composited; census held
  148/158 throughout every round (this is the load-bearing regression guard —
  **never let it move without investigating**).
- **r20**: explicit pipeline layouts (patch 0103) — fixed a real Dawn
  auto-layout-inference bug (depth textures inferred as filterable Float, rejected).
  Cube still didn't render; UI+splash did.
- **r21**: **the visibility-first play** (worker-side Dawn error-capture callback,
  mirroring patch 0040's native precedent) found the REAL blockers in one boot:
  a multi-VBO vertex-binding gap (patch 0104) and a sampled-view dimension mismatch
  (patch 0105). First non-UI viewport pixels appeared (selection outline on click).
- **r22**: found + fixed a binding-257 dedup bug (patch 0106) and a spurious
  GPU-fallback dialog (patch 0107). **Also produced two WRONG diagnoses** later
  corrected by r23: claimed present was "flaky" (it wasn't — see below) and
  claimed `--python-expr` doesn't execute (it does — see below). **Lesson banked:
  the in-app Browser pane tab used for verification through r22 reports
  `document.visibilityState === "hidden"`, which pauses Chrome's rAF loop — this
  silently degrades verification and produced both wrong conclusions.**
- **r23**: **established a proper visible verification rig** — headed
  node-Playwright with the **BUNDLED Chromium** (the Playwright MCP tool's default
  `chrome` channel points at a broken stub — always use bundled). This rig gets
  `visibilityState: "visible"`, ~120 rAF ticks/sec unprompted, faithful CDP
  `page.screenshot` capture (the OffscreenCanvas transfer makes `toDataURL`/
  element-shot blank — **CDP screenshot is the only faithful capture method**).
  With this rig: **ran the golden comparator for the first time on a real capture.
  Workspace state: FAIL at 35.5% — and that 35.5% IS exactly the blank viewport
  interior; the UI chrome portion already matches the native golden.** Also
  corrected r22: python DOES execute (r22's "FINAL pass not reached" was a
  misdiagnosis — stdout is just block-buffered and never flushed, but side effects
  fire fine). Also found the REAL present-path bug: **725× per boot
  `"Destroyed texture ... WebgpuSwapChainTexture used in a submit"`** — the surface
  texture's per-frame lifetime is shorter than command buffers referencing it.
  Landed patch 0108 (RW-storage-buffer visibility fix, unrelated cascade, real
  bug, ~80 errors/boot → 0).
- **r24**: **fixed the present seam** (patch 0109 + a `platform_web/ghost` change):
  chose the sturdier "persistent offscreen backbuffer + render-pass-blit-to-surface"
  model over per-frame lifetime juggling — eliminates the whole Destroyed-texture
  error class **by construction** (725→0, verified). Also found and fixed a
  real latent bug: `GHOST_WindowWeb::swapBufferRelease()` was a stub returning
  failure that never delegated to the context — Blender's swap call was **never
  reaching the present at all** before this fix. **BUT: the gate capture
  regressed to 100% black** (from r23's 35.5%). r24 did a **rigorous A/B**:
  restored r23's exact GPU code into the tree, rebuilt, **also got 100% black on
  two different Chromium versions**. Conclusion: **the regression is NOT in the
  GPU lane** — it's something in the boot/payload path that changed between r23
  and r24's builds. **Prime suspect, strongly corroborated: the `requests` wheel
  install (commit `679ebbf`, landed between r23 and r24) introduced a NEW
  `ModuleNotFoundError: No module named 'js'`** during `bl_pkg` register —
  `urllib3.contrib.emscripten.fetch` unconditionally imports Pyodide's `js` /
  `pyodide.ffi` modules on `sys.platform=='emscripten'`, which our vanilla
  CPython doesn't ship. This likely **stalls or crashes the WM loop during boot**,
  which would explain both the present-model regression AND the black capture
  regardless of GPU code correctness.

### THE ACTION ITEM — do this first, it may unblock the entire M4 gate

**A fix for exactly this suspect ALREADY EXISTS and is ALREADY COMMITTED — but on
an UNMERGED branch.** The human ran a background task in a separate session
(`task_b2d77377`, "Resolve urllib3 emscripten js/pyodide import wall") which
produced commit **`7c1cda2`** on branch **`claude/friendly-elgamal-3189df`**:
*"python-shims: js + pyodide.ffi import-shims — bl_pkg register import chain
closed on wasm (option A)"*. This matches EXACTLY the driver-approved design from
earlier in this session: a **fail-on-use shim** (imports succeed silently so
`inject_into_urllib3()`/register completes; any actual use of the shimmed surface
raises a clear `RuntimeError` naming the real blocker, never a confusing one) plus
a `ledger/deferred.json` entry (`emscripten-http-transport`, milestone "M7",
pointing at the real future fix: a proper Pyodide-style fetch transport for the
platform's online-extensions feature).

**Your first three actions as the new driver:**
1. **Verify `7c1cda2`** independently (don't trust the branch name/commit message
   alone): `git show --stat 7c1cda2`, check it touches only
   `scripts/deps/python-shims/js.py` (+ maybe `pyodide.ffi` sibling),
   `ledger/deferred.json`, `notes/python-emcc605-probe.md`, `REUSE.toml`/
   `LICENSES/`. Confirm the shim is genuinely fail-on-use (read `js.py` — does
   `hasattr(js, "Worker")` return False at import time via a controlled
   AttributeError-subclass, while real attribute *use* raises RuntimeError? this
   was the exact design spec given).
2. **Merge/cherry-pick `7c1cda2` onto `agent/m2.5-python-boot`** (the working
   branch every worker uses — confirm with `git branch --show-current`, should
   read `agent/m2.5-python-boot`). A clean cherry-pick is likely (the file is new,
   low collision risk) but check for conflicts with any concurrent work on
   `scripts/deps/` or `ledger/deferred.json` (23 entries as of this file; the
   shim branch may have added a 24th — reconcile, don't silently drop either).
3. **Dispatch the gate round (call it r25)**: fresh relink of `build-wasm-windowed`
   (the shim's `.data` payload change needs a rebuild+repackage to reach the
   binary — the python-wasm harvest into `lib/wasm` then a relink, OR if the shim
   lives purely in `lib/wasm/lib/python3.13/site-packages` it may just need the
   `.data` repackaged without a full C++ relink — check `scripts/deps/wheels.sh`'s
   harvest target and whether `build-wasm-windowed`'s `.data` step re-reads it
   automatically or needs a `ninja` touch). Then: boot in the r23/r24-proven
   VISIBLE rig (headed Playwright, bundled Chromium, `:8123/windowed.html`),
   capture, run `bash sandbox/m4-golden-prep/compare_m4.sh <png> workspace
   1280x720`, report exit code + stats. **If it passes: that is the M4 gate
   measurement — issue `<promise>M4_FIRST_PIXELS</promise>` in the same message
   with the receipts, exactly per GOAL.md's "promise tags only with harness
   receipts in the same message" rule.** If it still fails, the A/B is now clean
   (payload isolated) — instrument further with the r23/r24 error-visibility
   pattern (worker-side Dawn error callback, already wired) to find what's still
   blank.

### Verification rig — copy this exactly, it took 3 rounds to get right

- **Do NOT use the in-app Browser pane for M4 GPU verification** — it reports
  `document.visibilityState: "hidden"` in this harness, which pauses
  `requestAnimationFrame` and produces misleading "flaky"/"black" symptoms that
  are environment artifacts, not app bugs (this cost r22 a wrong diagnosis).
- **Use headed node-Playwright with the BUNDLED Chromium**, not the `chrome`
  channel (that's a broken stub with no binary on this machine). Load via
  `ToolSearch` for `mcp__plugin_playwright_playwright__*` if not already loaded.
- Capture via **CDP `page.screenshot`**, not `canvas.toDataURL()`/element
  screenshot — the WebGPU surface is a transferred `OffscreenCanvas` on a
  pthread worker; standard JS readback APIs return blank. CDP's compositor-level
  capture is faithful (proven in r23/r24 evidence).
- Server: `BLENDER_WEB_BIN=build-wasm-windowed/bin scripts/serve-web.sh 8123` →
  `:8123/windowed.html`. **Port map (don't collide):** 8123 = windowed dev (THE
  gate tree), 8124 = windowed-opt (`build-wasm-windowed-opt/`, size/shrink
  probes), 8125 = staged-loading probe, 8130 = `sandbox/gpu-render-harness`
  (separate small offscreen-render harness, unrelated to the windowed gate),
  8131 = `sandbox/m7-store-probe` / `sandbox/opfs-probe`.
- Error visibility: a worker-side Dawn `uncapturederror` callback is already wired
  in `platform_web/shell/wgpu-preinit-worker.js` (permanent, from r21/r23) —
  errors print to console; `grep -a` if capturing to a file (threaded wasm stdout
  gotcha, standing project-wide issue).
- Fresh-profile capture: clear OPFS/site state before capture so the Quick Setup
  splash variant matches the golden's fresh-profile assumption
  (`BLENDER_USER_CONFIG=<empty>` env trick, documented in
  `sandbox/m4-golden-prep/notes` / `notes/m4-golden-prep.md`).
- Goldens: **4 native captures, byte-identical across independent runs**, staged
  at `sandbox/m4-golden-prep/goldens/` (1280×720 and 1800×1001 — NOT 1169, the
  capture host's Retina display clamps window height; 1280×720 is the operative
  size the M4.T14 ghost-web `f19a96c` commit already ties the surface to).
  Comparator: `sandbox/m4-golden-prep/compare_m4.sh <candidate.png> <splash|
  workspace> <WxH>` — verbatim upstream `oiiotool --fail 0.016 --failpercent 1
  --diff`, self-tested with teeth (identity PASS, cross-state FAIL at 24%,
  RGBA-normalization for `toDataURL`-style candidates, size-mismatch FAIL).
  **Driver decision already made: the M4 gate compares the WORKSPACE state only**
  — the splash state has a structural, non-GPU delta (missing "Language: English
  (US)" row, `WITH_INTERNATIONAL=OFF` in our config) that isn't gate-relevant;
  don't re-litigate, don't re-cut that golden, just don't gate on it.

## Dead subagents this session — redispatch verbatim, both hit the SAME rate-limit wall at ~0% progress

Both died with `"You've hit your weekly limit · resets Aug 12 at 10am
(America/New_York)"` almost immediately after starting — **zero committed work,
zero partial artifacts on disk** (verified: no `scripts/deps/opensubdiv.sh`
exists; the `build-wasm-windowed-opt/bin/*.wasm` files have OLD mtimes from the
prior size-probe run, not fresh). **These are not resumable — redispatch as new
tasks with the prompts below (lightly updated for current state).** If this
session/pool is still rate-limited, note it and move to other work (see "What
else is ready to dispatch" below) — don't burn turns retrying a hard rate limit.

### 1. OpenSubdiv + IO rebuild round (M6 + M7, HIGH VALUE — unblocks two milestones' measurement at once)

The worker's only progress before dying: it discovered
`FindOpenSubdiv.cmake` requires BOTH `osdGPU` and `osdCPU` components even for a
CPU-only build (OpenSubdiv's own CMake always emits both targets regardless of
enabled backends) — factor this into the build recipe (build normally, both
targets will exist, just don't enable GPU compute backends via OpenSubdiv's own
`ONLY_CPU`-type options if it has one, or accept both static libs land in the
harvest).

Redispatch prompt (update the "inherited state" framing to reference this file
instead of the stale notes if you like, but the task body is still accurate):
recompile from `notes/m6-cycles-suite.md` (the 22/25-collapse-to-OpenSubdiv
proof) and `notes/m7-io-smoke.md` (the compiled-out exporters). Task: (1) cross-
compile OpenSubdiv to `lib/wasm` (CPU-only, TBB-linked, new
`scripts/deps/opensubdiv.sh`, idempotent, `ledger/deps.json` entry — Apache-2.0,
GPL-compat); (2) flip `patches/blender_web.cmake`: `WITH_OPENSUBDIV=ON` +
`WITH_IO_WAVEFRONT_OBJ/PLY/STL=ON` (USD stays OFF, separate future task; FBX/
grease-pencil-IO stay off); (3) reconfigure + relink `build-wasm-cycles/` (that
tree is free — the M6 Cycles-suite worker that used it is done); (4) re-measure:
`bash sandbox/m6-prep/run_wasm_cycles.sh` (target ~24/27, characterize the 3
residuals fresh — Embree-vs-BVH2 suspect), re-run `sandbox/m7-io-smoke`'s OBJ
export + `parse_obj.py` comparator against the staged native reference (glTF
stays blocked separately on the numpy rebuild below — don't touch numpy in this
task), and a one-line Subsurf-modifier smoke test (vert count vs oracle). File
ownership: `scripts/deps/opensubdiv.sh` (new), `lib/wasm` (opensubdiv harvest
only — **do not touch python/site-packages paths**, another workstream may still
be active there), `patches/blender_web.cmake` (the flip — **check `git log -1
patches/blender_web.cmake` first**, the shim-merge in action item #2 above may
have touched it too, reconcile), `build-wasm-cycles/**`, `sandbox/m6-prep/**` +
`sandbox/m7-io-smoke/**` (re-run outputs), notes, `ledger/deps.json` (append),
patches `0110`-`0119` if upstream needs `__EMSCRIPTEN__`-guarded touches
(standard rules: reverse-apply verified, `patches/series` updated). Off-limits:
`build-wasm-windowed` (+`-opt`), `platform_web/`, `gpu/webgpu` sources beyond
what a genuine compile error requires (characterize first), `harness/` (run-only),
`oracle/` (invoke-only), `tests/golden/`. Disk guard <8 GiB (currently 32 GiB
free — plenty of room; delete the OpenSubdiv build tree after harvest).

### 2. Wasm-shrink probe (M8, HIGH VALUE — the ≤15MB launch bar's residual lever)

The worker died before collecting ANY data (was still reading CMake cache
entries). Fully redispatchable as-is. Task: measure `-Os`, then `-Oz` if `-Os`
shows meaningful gains and time permits, then `--closure 1`, then `-flto` (expect
possibly 1h+ link with no `nice` available — non-root macOS `setpriority` is
denied on this machine, confirmed by the size-probe worker; if LTO's link exceeds
~90 min, abort and report it as needs-dedicated-CI rather than iteration-blocking)
— each against `build-wasm-windowed-opt/` (the free opt tree), each measured for
link time + wasm size (raw/gzip/brotli) + a correctness boot in the r23/r24
visible-rig pattern (must reach stable `WM_main` + clean `GPU_init` like the
`-O2` baseline; a boot regression is a FINDING to report, not silently a failure
to hide). Produce the verdict table + the combined projection (best-wasm-size +
the 4.5 MB staged-loading stage-0 data, `notes/m8-staged-loading.md`) against the
15 MB bar — state plainly whether compiler levers alone close the gap or whether
feature-audit/DCE work is needed next, and name that next step if so. File
ownership: `build-wasm-windowed-opt/**`, `notes/m8-wasm-shrink.md`,
`sandbox/size-probe/**` (append). **Take the tree's CURRENT CMake cache as
ground truth — do not reconfigure from `patches/blender_web.cmake` (another
workstream, action item #1 above, may be actively editing that file for the
OpenSubdiv/IO flip); use `cmake -C` cache-var edits instead, or flag if a
reconfigure becomes genuinely unavoidable.** Off-limits: `build-wasm-cycles`
(other workstream), `build-wasm-windowed` (reserved for the M4 gate round),
`platform_web/` (copy the shell if serving needs it, don't edit in place),
`upstream/` (read-only), `scripts/deps` + site-packages, `harness/oracle/
tests-golden`. Port **8124 only**. Commit per measured datapoint (a limit-kill
must not lose an already-measured number — this is exactly what happened last
time: zero datapoints committed because nothing was measured before the kill).

## What else is ready to dispatch (no blockers, can run immediately, zero collision with the above two or with the M4 gate round)

- **numpy `-DNDEBUG` rebuild** (small, isolated): fixes the glTF export blocker
  from `notes/m7-io-smoke.md`. Extend/add-to `scripts/deps/numpy.sh` for a
  Release-mode build (assertions off). Verify via `sandbox/m7-io-smoke`'s
  `parse_glb.py` comparator once done (that comparator is turnkey, per the
  worker's report). Touches `lib/wasm` python site-packages — **coordinate with
  whatever landed the urllib3 shim merge (action item #2), don't race it; do this
  AFTER the shim merge settles, or in the same combined python-payload pass.**
- **M6 workbench/EEVEE suites**: 50 goldens staged and oracle-green
  (`sandbox/m6-prep/`), completely blocked on M4's viewport composite (they need
  a live windowed GPU loop, unlike Cycles-CPU which runs headless under node).
  Do not dispatch until the M4 gate round reports a live viewport.
- **M5 session replay wiring**: 8 sessions ready
  (`sandbox/m5-prep/`), blocked on the same M4 windowed-loop dependency.
- **`.pyc` triplicate removal** (M8, free 40.8 MiB win, zero risk): proven safe
  by the staged-loading probe but not yet implemented in the actual packaging
  step. Small, isolated, low-risk — good filler task.
- **`patches/series` manifest**: already done (commit `5423913`, closes an audit
  MINOR finding) — just noting it's current if new patches (0110+) land, remember
  to extend it, don't forget.
- **Third audit pass**: due at ~25 more commits past `aec2568` (the second pass's
  range end) — you're well past that now (commits through `1f8f4a4` at this
  writing, roughly 45+ commits later). Worth dispatching once the M4 gate round
  and the two redispatched probes are in flight — adversarially review parity
  claims, patch hygiene, compliance, ledger honesty, same brief pattern as
  `reports/audit-20260806.md`.

## Standing operational rules (all still in force, unchanged)

- **Branch discipline**: everything happens on `agent/m2.5-python-boot`. The
  urllib3 shim is the one exception (separate branch, needs merging — see action
  item #2). Verify `git branch --show-current` before dispatching anything.
- **Verify, don't trust**: every worker's claim gets independently re-checked by
  YOU (the driver) before being relayed or acted on — reverse-apply the patches,
  spot-run the tests it claims pass, read the evidence files it cites. This
  session caught real problems this way multiple times (a stale dashboard number,
  a numpy site-packages clobber, patch-stack-order edge cases, two wrong
  diagnoses from a degraded verification environment).
- **Census guard**: `harness/run.sh --scope m3` (run-only for workers, you may
  also run `--regress`) must return **148/158 + static_shaders ≥956/973** after
  ANY change touching `gpu/webgpu`. This has held through 9 consecutive rounds
  (r16-r24) — treat any movement as a stop-the-line event, investigate before
  proceeding.
- **File ownership / no-collision discipline**: this session ran up to 5-7
  concurrent subagents successfully by strict path ownership per dispatch (see
  every redispatch prompt above for the exact boundaries). Never let two workers
  write the same file/tree concurrently. The shared trees are `build-wasm-
  windowed` (reserved for the M4 gate round only), `build-wasm-cycles` (free,
  OpenSubdiv+IO task claims it), `build-wasm-windowed-opt` (free, wasm-shrink
  task claims it), `lib/wasm` python site-packages (coordinate numpy rebuild
  after the urllib3 merge settles).
- **Patch numbering**: next free number is **0110** (0109 is the last landed,
  present-seam fix). The OpenSubdiv+IO task was told to use 0110-0119; adjust if
  another task claims that range first — just don't collide.
- **Ninja lock**: `scripts/ninja-locked.sh` serializes shared-tree builds;
  dedicated per-worker trees (the `-cycles`/`-opt` ones) may build directly at
  default priority (`nice` is unavailable on this machine — non-root
  `setpriority` denied, confirmed empirically).
- **Disk**: 32 GiB free as of this writing — healthy, but every worker prompt
  should still carry an <8 GiB abort guard (cheap insurance, costs nothing to
  include).
- **Compliance**: `reuse lint` clean modulo pre-existing gitignored build-tree
  noise (documented, not a regression). SPDX + provenance discipline has held
  throughout — every new file gets it, every commit gets `Assisted-by:` +
  `Co-Authored-By:` trailers matching the KA/Fable style used all session.
- **Dashboard**: `scripts/dashboard.sh` → `reports/dashboard.md` is truthful and
  live-sourced (fixed twice this session per audit findings — now sources GPU
  numbers from `ledger/results/m3.json`, not the stale hand-maintained note, and
  derives promise status from the literal `<promise>` tag string, not a
  substring grep). **Regenerate it (`bash scripts/dashboard.sh`) after the M4
  gate round lands**, whichever way it goes — the human uses this as their
  primary status view.

## Business/product context (from direct conversation with the human — not in git, record here so it isn't lost)

- **Timeline pressure is real and the pace has earned it**: the human has pushed
  hard on "2 more days" for a usable/demo-able artifact, given the fleet went
  from "one validation error from first frames" to M3-promised +
  UI-interactive-in-a-tab in ~2 days. The honest driver position (stated
  repeatedly and should be maintained): "start using it" (M4 gate + basic
  interaction) is genuinely 24-48h-scale AT THIS ROUND (the M4 gate is one
  boot-payload fix away, possibly already merged by the time you read this); the
  FULL launch gate (M8, all milestones) is 1-2 weeks at this sustained pace, not
  the original 3-5 week estimate — driven down by how much prep work has already
  landed (M5 sessions, M6 goldens+first-render, M7 store design+substrate, M8
  size data) ahead of when each milestone formally opens.
- **Faster models (DeepSeek/Groq/Cerebras) were explicitly evaluated and
  rejected** as a lever: the bottleneck is machine wall-clock (link times,
  boot+measure cycles) and the discovery loop (each fix reveals the next only
  after a boot), not token throughput. This should NOT be re-litigated unless
  something structural changes (e.g., a genuinely parallel build farm).
  Levers that DO help, in order the human was told: (1) more disk (currently
  32 GiB, healthy — was capping build-tree parallelism earlier at ~13-26 GiB);
  (2) a second machine / CI runners for long builds (wasm64 rebuild, LTO probes,
  soak test) to run off this Mac entirely — **not yet provided, still an ask**;
  (3) the public brand name decision (D-7, blocks ANY public/open-source repo
  push — **still not decided, still blocking**); (4) correct scoping — split
  "usable demo" (near) from "full launch gate" (M8, weeks) rather than treating
  them as one deliverable.
- **A concrete demo-video script was written** (in-conversation, not committed
  to a file — worth committing to `notes/` or a `LAUNCH.md` appendix if you have
  spare cycles) — a ~75-second continuous take: load → splash → default scene →
  select/G/R/S → orbit/zoom → Tab edit-mode/extrude → Add Torus from the real
  menu → DevTools Network-tab-silent + offline toggle (the "native, not
  streamed" differentiation proof per D-8) → end card. This is scriptable the
  moment M4 + basic G/R/S/Tab interaction verification lands (soon).
- **A platform-integration vision was discussed and designed** (see
  `notes/platform-integration-design.md`): the human wants an account-based
  model library on a separate web-game-engine platform, with "Open with Blender"
  deep-linking, auto-save to account, recent-files list — all of this maps
  cleanly onto the M7 OPFS store (now proven both-halves) with **zero upstream
  patches**, arm's-length via URL params/postMessage per the GPL boundary. This
  is real product surface the human cares about beyond the port itself — keep it
  in mind when M7 code lands (it should be built with this integration pattern
  in mind, matching the design note).
- **The human explicitly does NOT want more subagents deployed right now** (this
  exact handoff was requested instead, "don't deploy any more subagents") —
  respect that instruction into the next session too, until they say otherwise;
  this handoff is being generated in lieu of continuing dispatch.

## One-paragraph summary if you only read one thing

M0-M3 are done with harness receipts (`harness/status.sh` is ground truth).
Blender's real UI has been rendering upright and interactive in a Chrome tab for
several rounds now; the M4 gate (3D viewport compositing the default cube,
matching a native golden within Blender's own idiff threshold) is blocked on
exactly one thing — a boot-time Python import wall (`urllib3`'s emscripten path
needing Pyodide's `js` module) that likely stalls the WM loop — **and a fix for
it already exists, already committed, on branch `claude/friendly-elgamal-3189df`
commit `7c1cda2`, just not yet merged into the working branch.** Merge it, relink,
capture in the visible-Playwright-rig (not the in-app Browser pane — it's hidden/
throttled and gives false readings), run the pre-staged comparator
(`sandbox/m4-golden-prep/compare_m4.sh`), and either issue the M4 promise or
instrument further with the already-wired error-visibility tooling. In parallel,
M6 got its first real rendered image (Cycles-CPU, oracle-exact, zero patches) and
measured its suite at 2/27-but-provably-collapsing-to-one-missing-dependency
(OpenSubdiv); M7 fully designed and proved its file-storage substrate with zero
upstream patches needed; M8 measured the launch size at 47.5 MB wire, staged it
down to 23.6 MB, and found the wasm binary itself is the residual blocker to the
15 MB bar. Two probes for the OpenSubdiv/IO fix and the wasm-shrink measurement
died at 0% progress on a subagent rate limit (resets Aug 12) and are fully
redispatchable verbatim from the prompts above. Nothing architectural remains
unknown anywhere in the project — every open item above is either dispatched,
ready to dispatch, or waiting on one merge.
