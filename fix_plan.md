# fix_plan.md — current milestone: M0 TOOLCHAIN + ORACLE
- [ ] M0.1 DONE 2026-08-03: upstream at pin fbe6228777e7 (tip of blender-v5.2-release), LFS assets deferred as pointers (lfs.url set to projects.blender.org for later `git lfs pull --include assets/`).
- [ ] M0.2 DONE 2026-08-03: emsdk latest installed at tools/emsdk (gitignored); emcc 6.0.5 (>=4.0.10 OK), node 22.16.0, release dbd755b5 — recorded in oracle/TOOLCHAIN. hello.c->wasm runs in node ("hello from wasm"); emdawnwebgpu port resolves+compiles (--use-port=emdawnwebgpu -c; cache/ports/emdawnwebgpu.175430.zip). RESIDUAL: in-browser smoke page NOT done here (needs a served COOP/COEP page) — fold into M0.5 harness / M4 first-pixels.
- [x] M0.3 Oracle online: pinned native Blender 5.2.0 headless on this host (macOS arm64 official archive under oracle/, or Docker linux image); oracle/bpy.sh runs `blender -b --python-expr`; verify version string == 5.2.0; install oiiotool (brew openimageio) for image diffs. — DONE 2026-08-03: Blender.app (official macos-arm64 dmg) copied to oracle/blender-5.2.0/Blender.app; bpy.sh BLENDER_BIN → .../Contents/MacOS/Blender; `--version` = "Blender 5.2.0 LTS (hash fbe6228777e7 built 2026-07-14)"; smoke `bpy.data.objects.keys()` = ['Camera','Cube','Light']; oiiotool 3.1.16.0 (brew openimageio) at /opt/homebrew/bin.
- [ ] M0.4 harness/buildwrap.sh proven: wraps an emcc build, one-line success, first-50-errors failure summary, full log under ledger/buildlogs/.
- [ ] M0.5 harness/run.sh v1 + status.sh: --scope m0 runs toolchain+oracle smokes and writes ledger/results/*.json; then `touch .claude/harness.lock` to activate write-protection on harness/.
- [ ] M0.6 DONE 2026-08-03: patches/blender_web.cmake drafted (77 WITH_* options). Union of blender_lite + blender_headless, then GOAL overrides: WITH_CYCLES ON but CPU-only (all _DEVICE_*/_BINARIES/OSL/EMBREE/PATH_GUIDING OFF); USD/HYDRA/MATERIALX/OPENVDB/MOD_FLUID/LLVM/XR/ALEMBIC/audio/NDOF/INTERNATIONAL/native-GHOST OFF; WITH_PYTHON/WITH_TBB/WITH_HEADLESS ON. Every name verified in upstream/CMakeLists.txt at pin; WITH_BOOST dropped (no longer an option). Forced-OFF deps recorded under "forced_off" in ledger/deps.json.
- [ ] M0.7 DONE 2026-08-03 (skeleton): LICENSES/{GPL-2.0-or-later,GPL-3.0-or-later,CC0-1.0}.txt (canonical SPDX via `reuse download`), LICENSE, NOTICE, PROVENANCE.md, THIRD-PARTY.md, REUSE.toml created; reuse 6.2.0 installed (pipx + charset-normalizer inject). All authored files pass reuse lint (0 missing/unused; 3 licenses used). FOLLOW-UP for full-repo green (BLOCKED, out of scope): 22 pre-existing scaffold files (GOAL/LAUNCH/CLAUDE/SETUP/harness/scripts/oracle/sandbox/loop/worker) need SPDX or a REUSE.toml block; GOAL.md prose emits 1 invalid SPDX expression that needs a REUSE-Ignore marker *inside* GOAL.md (edit-forbidden per thrift rule 5).
- [ ] M0.8 Ops: disk audit (claude -p re-verified WORKING 2026-08-03; workers = sonnet via claude + codex; driver = opus) (need >=40GB free before M2 — currently ~19GB [M0.2 2026-08-03 measured only ~10.6GiB free after emsdk ~1.7GB install — WORSE than ~19GB; emcc/EM_CACHE/emdawnwebgpu now on this volume]: identify reclaim or external volume; PAGE if unresolvable); write notes/codex-cli.md from `codex exec --help` (verify non-interactive + model flags for worker use).
- [ ] M0.9 (deferred until GitHub repo exists) CI skeleton — local-first for now; revisit at M1 exit.
