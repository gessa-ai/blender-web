# Audit R10 canonical receipt freshness

## Outcome

Commit `421e610` makes the fixed-path upstream freeze receipt an accepted part of canonical
replay instead of unvalidated historical JSON. `sandbox/series-replay/verify.py` now publishes
`CANONICAL_REPLAY_PASS` only after the receipt binds the current patch bytes and SHA-256, Blender
pin, current pin-file content, exact freezer schema/check set, and byte-identical live/replay
manifest identities. The receipt's absolute source paths remain provenance rather than a checkout
identity, so a byte-identical clone can verify the proof without regenerating machine-specific
evidence.

The retained receipt was regenerated by `sandbox/final-source-freeze/freeze.py`; it was not
edited into apparent success. Its generated `canonical-source.patch` is byte-identical to
`patches/PREVIEW_SNAPSHOT.patch`, and the generated receipt is byte-identical to the retained
fixed-path JSON. The current authority is 2,339,419 bytes at SHA-256
`1eade204c8420b2c520109a5cea550a790f34000d650b6f5457494eec6785462`, with 20,258-entry
live/replay manifests at SHA-256
`b7e79677ee7f3a40dee6195423a113251dcb3ed69fd5aeb31e584ed15027af89`.

## Evidence

- The predecessor verifier passed while the fixed receipt named patch SHA-256 `ba8191cb...` and
  the current patch was `1eade204...` (`20260825T113310-1213746`). With the guard applied and the
  stale receipt still present, verification rejected before replay with `patch byte count is
  stale` (`20260825T113701-1215961`).
- The complete one-root freezer passed its pin, pristine-index, clean-submodule, replay,
  byte-identical manifest, and final resnapshot checks (`20260825T113838-1217340`). Exact patch
  and receipt comparisons pass (`20260825T114435-1222884`, `20260825T114435-1222885`).
- Post-commit canonical replay passes 303 paths, 254 active numbered-history identities, current
  patch prefix `1eade204c842`, and retained receipt prefix `2e9f022865ad`
  (`20260825T114417-1222663`).
- The hermetic self-check rejects 18 stale-hash, stale-size, schema, verdict, key-set, pin,
  provenance, timestamp, manifest, freezer-check, malformed-JSON, and changed-patch mutations
  (`20260825T114417-1222664`).
- REUSE 6.2.0 covers 2,522/2,522 files (`20260825T114417-1222665`).
- Required M3 remains honestly RED only at the absent s7-blocked strict candidate
  (`20260825T114525-1223976`). Container-backed regression restores M0 6/6 GREEN while M1-M8
  retain their existing strict receipt/browser/product/release boundaries
  (`20260825T114528-1224023`).

This is source-evidence plumbing only. It creates no adapter, device, surface, browser/pixel
receipt, profile, split product, result promotion, tolerance, golden, blacklist, or milestone
promise. The hardware boundary remains **no conformant hardware Vulkan ICD in WSL2 (NVIDIA ships
none; Mesa dzn rejected by Dawn)**; dzn and Windows were not attempted and WSL was not restarted.
