diff --git a/source/blender/gpu/webgpu/wgpu_context.cc b/source/blender/gpu/webgpu/wgpu_context.cc
--- a/source/blender/gpu/webgpu/wgpu_context.cc
+++ b/source/blender/gpu/webgpu/wgpu_context.cc
@@ -1267,5 +1267,5 @@ void WGPUContext::sync_backbuffer()
    * while the attachment keeps pointing at this same WGPUTexture wrapper. */
   back->adopt_external(backbuffer, fmt, w, h);
-  redraw_present_frame_episode_ = backbuffer_snapshot.redraw_episode;
+  redraw_present_adopted_episode_ = backbuffer_snapshot.redraw_episode;
 
   if (first) {
@@ -1306,6 +1306,8 @@ void WGPUContext::begin_frame()
 {
 #if defined(__EMSCRIPTEN__) && !defined(WITH_HEADLESS)
-  /* Activation already adopted this frame's backbuffer episode. Reset its semantic trace once at
-   * the real WM frame boundary; later context reactivations must not erase earlier region draws. */
+  /* Capture the drawable episode once at the real WM frame boundary. A resize can commit and a
+   * context can reactivate later in this same frame; neither may relabel work that began against
+   * the prior drawable. */
+  redraw_present_frame_episode_ = redraw_present_adopted_episode_;
   ghost_web::redraw_trace_frame_begin(redraw_present_frame_episode_);
 #endif
diff --git a/source/blender/gpu/webgpu/wgpu_context.hh b/source/blender/gpu/webgpu/wgpu_context.hh
--- a/source/blender/gpu/webgpu/wgpu_context.hh
+++ b/source/blender/gpu/webgpu/wgpu_context.hh
@@ -331,8 +331,9 @@ class WGPUContext : public Context {
   /** Serializes async validation with queue writes/submissions without changing their order. */
   webgpu::OrderedQueueScheduler queue_scheduler_;
 #if defined(__EMSCRIPTEN__) && !defined(WITH_HEADLESS)
-  /** Resize episode adopted atomically with this window frame's persistent backbuffer. A
-   * replacement can settle while the frame is encoding; only an adopted frame from the current
-   * episode may place that episode's completed-frame barrier at its queue tail. */
+  /** Resize episode most recently adopted atomically with the persistent backbuffer. */
+  uint64_t redraw_present_adopted_episode_ = 0;
+  /** Snapshot of the adopted episode at begin_frame(). Later context reactivation may adopt a
+   * replacement drawable, but it must not relabel the frame already being encoded. */
   uint64_t redraw_present_frame_episode_ = 0;
 #endif
