# Stuck diagnosis — s7 hardware-Vulkan boundary

<!-- SPDX-FileCopyrightText: 2026 blender-web contributors -->
<!-- SPDX-License-Identifier: CC0-1.0 -->

Date: 2026-08-20 America/New_York (2026-08-21 UTC).

## Outcome

The current milestone is stopped at the same external s7 condition after more than three
consecutive iterations without harness-state progress: ornith-lab exposes only a CPU llvmpipe
Vulkan adapter. That adapter is useful as a rejection control but cannot bind an M3 device
receipt, an M4 CAPTURE profile, an APPLY split product, or any M4-M8 browser receipt. The stop
condition in `notes/wsl-vulkan-investigation-20260819.md` remains correct and no gate result,
receipt, profile, deferral, or promise is changed by this diagnosis.

The last harness-state improvement was M8 compliance, which reduced its diagnostics from 43 to
22 at `2026-08-20T22:20Z`. The following seven completed iterations hardened or ported producers
without changing a suite result: M7 files, M4 CAPTURE, the second audit, the shared runtime
adapter contract, M4 fullscreen, M8 split runtime, and M8 monolithic deployment. This exceeds
the three-iteration threshold in `GOAL.md`'s stuck protocol.

## Fresh host evidence

At `2026-08-21T00:50Z`, `vulkaninfo --summary` again enumerated exactly one device:

- `deviceType = PHYSICAL_DEVICE_TYPE_CPU`
- `deviceName = llvmpipe (LLVM 21.1.8, 256 bits)`
- `driverName = llvmpipe`, Mesa `26.0.8-1ubuntu0.3`

The account database includes `pc` in group `docker`, while this inherited process's kernel
group list does not. That explains the latest M0 oracle regression from 6/6 to 3/6; it is a
session-refresh prerequisite, not a source or oracle defect and not permission to alter the
protected harness. The strict M1 and M2 Linux receipts remain immutable and valid, but their
aggregate scopes still require the same complete fresh M0-M3 manifest that s7 prevents.

## Exhausted safe work

The CAPTURE producer, profile union, APPLY finalizer, M4 full-window tripwire, M3 Dawn/Vulkan
probe, strict M5-M8 browser producers, and all independent consumers now fail closed on software,
fallback, masked, or inconsistent adapters. Browser-free root/descendant, tool-identity,
confinement, mutation, and no-allocation checks are green. The windowed OFF product and the two
M3 probe targets are built through `scripts/ninja-locked.sh` and end at exact no-work.

Remaining macOS paths found outside those current producers belong to retained diagnostic rigs,
historical evidence, native-golden capture, or the intentionally macOS-only Safari row. Rewriting
them would not advance a current gate and would risk mutating evidence rather than portability.

## Split resume path

1. **External host state:** start a fresh login/session so the existing Docker group enrollment
   reaches the process, and restore/install a WSL-compatible hardware Vulkan ICD that enumerates
   the RTX 4090 through `/dev/dxg`. This requires host coordination outside repository scope.
2. **Acceptance control:** rerun `vulkaninfo --summary`; require a non-CPU hardware device. Then
   run the existing headed Chromium adapter proof and require one unmasked, non-fallback,
   non-software adapter before allocating evidence.
3. **M4 first:** execute the documented CAPTURE success and terminal-error scenarios, merge the
   exact profile union, relink APPLY only through `scripts/ninja-locked.sh`, run product preflight,
   capture a new immutable M4 label, bind it, and run the unchanged comparator plus `m4` scope.
4. **M3 second:** rebuild/replay Dawn/Vulkan against the exact checked-in 197-test and
   1,003-shader identities, including DrawWebGPU, OpenSubdiv, cold/warm cache, zero uncaptured
   errors, and final locked no-work. Do not rebaseline a platform delta silently.
5. **Aggregate last:** compose one fresh M0-M3 candidate, then run M5-M8 live producers against
   the exact APPLY artifact and finish with required scoped checks and regression.

Until step 1 changes external state, another software-adapter replay or another portability-only
pass over retained historical rigs is not a distinct fix attempt and must not create evidence.
