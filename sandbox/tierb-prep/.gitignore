# scratch run artifacts (raw logs, generated .blend) — not committed
_out/
_datafiles_wasm/
# composed datafiles payload (symlinks -> release/datafiles + upstream/assets); the
# dir name MUST end in `datafiles` so is_smooth_by_angle_modifier's endswith matches
# (notes/m2-object-edit-autosmooth.md). Regenerated by run_suite_wasm.sh; never committed.
datafiles/
